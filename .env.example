# geo-audit — environment variables
#
# Copy this file to .env and fill in only the keys you have. Missing keys
# cause modules to degrade gracefully — the audit still runs and tells
# you exactly what each missing key would unlock.
#
# .env is gitignored — never commit your real keys.
#
# Run `geo-audit doctor` after editing .env to see which modules become
# active.


# ── REQUIRED for full LLM-grade audits (pick AT LEAST ONE) ──────────
# An LLM key unlocks:
#   - LLM-graded citability + content E-E-A-T scoring
#   - Brand-mention scan in the corresponding LLM
# Cost: ~$0.001 per audit on Haiku / gpt-4o-mini. Cheap.

# Anthropic — Claude (Haiku-class is fastest for our workload).
# Get one: https://console.anthropic.com/settings/keys
# Free tier: no — but $5 minimum credit lasts hundreds of audits.
ANTHROPIC_API_KEY=

# OpenAI — ChatGPT.
# Get one: https://platform.openai.com/api-keys
# Free tier: no — but gpt-4o-mini is ~$0.001 per audit.
OPENAI_API_KEY=


# ── HIGHLY RECOMMENDED (free tier available) ────────────────────────

# Google PageSpeed Insights — Core Web Vitals (LCP/INP/CLS).
# Get one: https://developers.google.com/speed/docs/insights/v5/get-started
# Free tier: yes — 25,000 queries/day free.
PAGESPEED_API_KEY=

# Google Gemini — proxy for Google AI Overviews readiness.
# Get one: https://ai.google.dev/gemini-api/docs/api-key
# Free tier: yes — generous.
GEMINI_API_KEY=

# Perplexity — live brand-mention check in Perplexity AI search.
# Get one: https://www.perplexity.ai/settings/api
# Free tier: yes — $5 free credits on signup.
PERPLEXITY_API_KEY=


# ── OPTIONAL: Web search backends (for grounded brand-mention scans) ─

# Tavily — LLM-friendly web search API.
# Get one: https://tavily.com
# Free tier: yes — 1,000 searches/month free.
TAVILY_API_KEY=

# Serper — Google SERP wrapper. Use INSTEAD of SearXNG, not both.
# Get one: https://serper.dev
# Free tier: yes — 2,500 queries free, then paid.
SERPER_API_KEY=

# SearXNG — self-hosted SERP backend (no auth, no key — just a URL).
# Setup: https://docs.searxng.org/admin/installation.html
# Set this OR SERPER_API_KEY.
SEARXNG_BASE_URL=


# ── OPTIONAL: Russian-language market ───────────────────────────────

# Yandex.XML — Russian-market AI search visibility (Yandex Neuro proxy).
# Get one: https://xml.yandex.ru/
# Free tier: yes — limited daily quota.
# Both fields required.
YANDEX_XML_USER=
YANDEX_XML_KEY=


# ── OPTIONAL: Paid AI-content detection ─────────────────────────────
# (Default: free local heuristic. These add a paid third-party signal.)

# Originality.ai — pay-per-scan AI detection.
# Get one: https://originality.ai/
ORIGINALITY_API_KEY=

# GPTZero — limited free tier + paid plans.
# Get one: https://gptzero.me/
GPTZERO_API_KEY=


# ── Cache + telemetry settings (rarely changed) ─────────────────────

# Where to write the HTTP cache. Default: ~/.cache/geo-audit/
# GEO_AUDIT_CACHE_DIR=

# Notes
# -----
# - All keys live ONLY on your machine.
# - geo-audit makes calls only to the documented endpoints (see TRUST.md).
# - There is NO HWAI relay, NO telemetry, NO phone-home.
# - Read scripts/agent-preinstall-check.sh before running install.sh.
